"use client";
import React from "react";

const ProjectsCarousel = () => {
  return (
    <section
      className="flex items-center justify-center min-h-screen w-full px-4 py-8"
      style={{ background: "var(--accent-orange)", color: "var(--lime-green)" }}
    >
      <div className="text-center">
        <h2 className="text-3xl font-semibold mb-4">Projects</h2>
        <div className="w-full h-40 flex items-center justify-center border-2 border-dashed border-gray-300">
          [Project carousel placeholder]
        </div>
      </div>
    </section>
  );
};

export default ProjectsCarousel; 