// Utility function to get the correct base path for assets
// Next.js handles base path automatically through next.config.ts
export const getAssetPath = (path: string): string => {
  return path;
}; 